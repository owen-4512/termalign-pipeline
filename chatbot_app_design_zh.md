# 面向学生课外外语学习的 ChatGPT API 电脑端聊天应用设计方案

## 1. 项目目标

构建一个可在 **Windows / macOS（可扩展 Linux）** 上使用的桌面 Chatbot 应用，核心目标：

1. 学生端：
   - 清爽干净界面，低学习成本。
   - 支持多语学习场景（词汇、语法、口语、写作纠错、翻译对比、情景对话）。
   - 支持对话历史检索、收藏、复习。
2. 管理端（后台）：
   - 管理员可查看与管理聊天记录。
   - 按班级/学生/时间筛选，支持审计与导出。
   - 支持基础运营统计（活跃人数、提问主题、学习时长）。
3. 部署与下载：
   - 提供安装包下载（.exe / .dmg）。
   - 自动更新机制（减少维护成本）。

---

## 2. 推荐技术架构（兼顾开发效率与可维护性）

> 推荐采用 **Electron + Web 前端 + 后端 API 服务** 的三层架构。

### 2.1 总体结构

- **桌面端（学生端）**：Electron + React + TypeScript + TailwindCSS
- **后端服务**：Node.js (NestJS) 或 Python (FastAPI)
- **数据库**：PostgreSQL（主库） + Redis（缓存/会话）
- **对象存储（可选）**：S3/OSS（用于导出文件、语音附件等）
- **AI 接入层**：调用 OpenAI ChatGPT API（建议 Responses API）
- **管理后台（Web）**：React + Ant Design（只给管理员）

### 2.2 为什么适合你的场景

- Electron 适合快速做“可下载安装”的跨平台桌面应用。
- Web 技术栈便于 UI 打磨和后续功能迭代。
- 前后端分离便于权限隔离（学生端与管理员端不同能力）。
- PostgreSQL 便于存储结构化聊天数据和统计分析。

---

## 3. 核心功能设计

## 3.1 学生端功能

1. **聊天学习模式**
   - 自由问答模式：学生直接提问。
   - 任务模式：词汇闯关、语法练习、情景对话（点餐/面试/旅游）。
2. **语言学习增强**
   - 一键“更地道表达”
   - 一键“纠错并解释”
   - CEFR 难度控制（A1-C2）
   - 中英/多语对照显示
3. **学习记录与复习**
   - 会话自动归档（按日期、主题）
   - 收藏重点句子
   - 错题/高频错误总结
4. **安全与提示**
   - 敏感内容过滤（校园场景）
   - 学习免责声明与使用规范

## 3.2 管理后台功能

1. **用户与组织管理**
   - 学生账号、班级、角色权限（RBAC）
2. **聊天记录管理**
   - 按学生、班级、时间段查询
   - 按关键词检索
   - 导出 CSV/Excel/PDF
3. **统计看板**
   - DAU/WAU、平均对话轮次、学习时长
   - 热门主题（词汇/语法/口语）
4. **内容安全与审计**
   - 违规对话告警
   - 敏感词命中日志
   - 管理员操作日志

---

## 4. 清爽干净的 UI 方案（学生端）

## 4.1 视觉原则

- 主色：低饱和蓝/绿（学习感、可信）
- 大量留白 + 卡片式信息布局
- 字体层级明确（标题、正文、注释）
- 动效轻量（150~250ms）

## 4.2 页面结构

1. **左侧边栏**：会话列表、搜索、收藏
2. **中间主区域**：聊天消息流
3. **右侧学习面板**（可折叠）：
   - 语法点
   - 词汇本
   - 今日学习目标

## 4.3 关键交互

- 输入框内置快捷按钮：
  - 纠错
  - 改写
  - 翻译
  - 生成练习题
- 每条 AI 回复支持：
  - 复制
  - 收藏
  - 标注“没看懂”并请求简化

---

## 5. 数据模型（简版）

## 5.1 主要表

- `users`（学生/老师/管理员）
- `classes`（班级）
- `conversations`（会话）
- `messages`（消息，含 user/assistant/system）
- `learning_tags`（语法、词汇、口语标签）
- `bookmarks`（收藏）
- `audit_logs`（后台操作日志）

## 5.2 关键字段建议

- `messages`：
  - `conversation_id`
  - `role`
  - `content`
  - `language`
  - `tokens_used`
  - `safety_flag`
  - `created_at`

---

## 6. ChatGPT API 接入建议

1. **服务端代理调用，不在客户端直连 API**
   - 避免泄露 API Key。
2. **系统提示词模板化**
   - 针对“外语学习助教”固定教学风格：鼓励、纠错清晰、分步骤解释。
3. **上下文策略**
   - 最近 N 轮 + 关键记忆摘要，避免 token 暴涨。
4. **成本控制**
   - 长对话自动摘要
   - 模型分级（普通问答用轻量模型，复杂写作用高质量模型）
5. **可观测性**
   - 记录请求耗时、token、错误码，便于优化。

---

## 7. 权限与安全设计

1. **认证**：邮箱/学号 + 密码（建议再加 SSO 可选）
2. **鉴权**：RBAC（student / teacher / admin）
3. **数据隔离**：
   - 学生只能看自己的会话
   - 管理员按机构权限查看
4. **合规与隐私**：
   - 明示数据用途
   - 支持用户数据导出与删除
   - 传输 HTTPS，全库加密（至少敏感字段加密）

---

## 8. 下载与发布方案（轻松下载安装）

1. **打包工具**：electron-builder
2. **输出产物**：
   - Windows: `.exe` 安装包
   - macOS: `.dmg`
3. **自动更新**：
   - Electron Auto Updater
   - 启动时静默检查更新
4. **分发方式**：
   - 学校官网下载页 / 企业网盘 / 私有发布页

---

## 9. 推荐实施路线（12 周示例）

1. 第 1-2 周：需求梳理 + 原型设计（学生端/后台）
2. 第 3-4 周：账号体系 + 会话基础功能
3. 第 5-6 周：ChatGPT API 接入 + 学习工具按钮
4. 第 7-8 周：后台管理（查询、导出、统计）
5. 第 9-10 周：安全、审计、性能优化
6. 第 11 周：打包与自动更新
7. 第 12 周：试点班级灰度上线

---

## 10. MVP（第一版）建议

如果你想尽快上线，第一版只做：

- 学生聊天 + 历史记录
- 管理员查看聊天记录（检索 + 导出）
- 基础纠错/翻译快捷按钮
- Windows 安装包

这样通常可在 **6~8 周** 内完成首发，再逐步加统计看板、学习路径、语音功能。

---

## 11. 你可以直接给开发团队的需求摘要（可复制）

- 做一个 Electron 桌面 Chatbot（Windows/macOS），界面风格清爽简洁。
- 学生端要支持外语学习场景：问答、纠错、翻译、改写、情景对话。
- 后端用服务端调用 ChatGPT API，保存会话和消息到 PostgreSQL。
- 需要管理员后台：按学生/班级/时间检索聊天记录，可导出。
- 要有基础统计和审计日志。
- 产出可下载安装包，并支持自动更新。

