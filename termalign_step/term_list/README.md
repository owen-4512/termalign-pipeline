# termalign term list

默认术语词典（当 CLI 未传 `--dict-zh-path/--dict-en-path` 或 `--dict-zh/--dict-en` 时自动使用）：

- `zh_terms.txt`：中文术语列表（每行一个术语）
- `en_terms.txt`：英文术语列表（每行一个术语）

如果文件存在且参数未显式传入，termalign 会自动读取。
